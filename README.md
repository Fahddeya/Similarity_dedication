# Similarity_dedication
C++ program for string set similarity detection. Supports loading from files/strings, set operations (add, remove, clear), and calculates union, intersection, and similarity scores
